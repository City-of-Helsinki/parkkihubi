# Parkkidash -- Parkkihubi Dashboard

This project uses React + TypeScript with Vite.

## Available scripts

- `pnpm start` starts the Vite development server.
- `pnpm build` runs TypeScript type-checking and builds production assets with Vite.
- `pnpm test` runs tests with Vitest.
